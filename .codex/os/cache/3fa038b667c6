# Example 01 — PDF → LinkedIn Carousel

**User**

> Convert this whitepaper PDF into a LinkedIn carousel.

**Skill Hunter**

```
Aga, bu is icin hazir bir skill/tool kullanilabilir gibi gorunuyor 😎

Best candidate:
- Name: pdf-extract + python-pptx
- Type: CLI + library combo
- What it does: extracts text/images from PDF, generates slide deck programmatically
- Why it fits: mature, official-ish, no API key, offline, produces PPTX which LinkedIn accepts as carousel upload
- Risk: low — both libraries are well-maintained and local-only
- Effort: ~30 lines of glue code
- Recommendation: use it

Use this?
1. Yes, use it
2. No, build manually
3. Show alternatives
```

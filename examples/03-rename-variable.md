# Example 03 — Rename Variable (skill discovery skipped)

**User**

> Rename `userId` to `accountId` in `src/auth/session.ts`.

**Skill Hunter**

Skill Discovery Pass **skipped** — task is trivial and the project already has an internal path (IDE/LSP rename, `sed`, or direct edit). Searching would take longer than doing it.

Proceeding with the edit directly.
